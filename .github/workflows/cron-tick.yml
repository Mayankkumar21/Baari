name: cron-tick

# Hits Baari's /api/cron/tick endpoint every 5 minutes.
# GitHub Actions is the scheduler because it runs regardless of host —
# same job worked when we were on Vercel and works now on Railway.
#
# Required GitHub repository secrets (Settings → Secrets and variables → Actions):
#   APP_URL       your deployed origin, e.g. https://getbaari.in (no trailing slash)
#   CRON_SECRET   matches the CRON_SECRET env var set on the deploy host
#
# Note: GitHub Actions scheduled workflows can be delayed by up to ~10 minutes
# under load. Acceptable for no-show sweeps; end-of-day auto-close still fires
# within its window.

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: {}

jobs:
  tick:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: GET /api/cron/tick
        env:
          APP_URL: ${{ secrets.APP_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          if [ -z "$APP_URL" ] || [ -z "$CRON_SECRET" ]; then
            echo "::error::APP_URL or CRON_SECRET secret is missing"
            exit 1
          fi
          curl --fail --silent --show-error \
            "$APP_URL/api/cron/tick" \
            -H "Authorization: Bearer $CRON_SECRET" \
            --max-time 30
